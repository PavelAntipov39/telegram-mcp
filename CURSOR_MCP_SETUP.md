# 🔧 Настройка Telegram MCP для Cursor

## ✅ Конфигурация создана!

Файл конфигурации MCP для Cursor создан: `~/.cursor/mcp.json`

## 📋 Что нужно сделать

### 1. Перезапустите Cursor

После создания конфигурации необходимо **полностью перезапустить Cursor**:
- Закройте Cursor полностью (Cmd+Q на Mac)
- Откройте Cursor снова

### 2. Проверьте переменные окружения

Убедитесь, что файл `.env` в директории проекта содержит:
```
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_SESSION_STRING=ваш_session_string
```

Переменные окружения автоматически загружаются из `.env` файла при запуске через `uv run main.py`.

### 3. Проверьте подключение

После перезапуска Cursor попробуйте спросить:

```
Покажи мои последние чаты в Telegram
```

или

```
Список моих Telegram чатов
```

## 🎯 Примеры использования

После успешной настройки вы сможете использовать Telegram прямо в Cursor:

### Просмотр чатов
- "Покажи мои последние чаты"
- "Список моих групп в Telegram"
- "Найди чат с [имя]"

### Чтение сообщений
- "Покажи последние сообщения из чата [название]"
- "Что нового в группе [название]?"
- "Прочитай непрочитанные сообщения"

### Отправка сообщений
- "Отправь сообщение 'Привет!' в чат [название]"
- "Напиши в группу [название]: [текст]"

### Поиск
- "Найди сообщения с текстом 'важное'"
- "Покажи все чаты с [имя контакта]"

## 🔍 Проверка работы MCP

Если MCP не работает:

1. **Проверьте логи Cursor:**
   - Откройте Developer Tools (View → Developer → Toggle Developer Tools)
   - Проверьте консоль на наличие ошибок

2. **Проверьте конфигурацию:**
   ```bash
   cat ~/.cursor/mcp.json
   ```

3. **Проверьте, что uv доступен:**
   ```bash
   which uv
   /Users/pavelantipov/.local/bin/uv
   ```

4. **Проверьте путь к проекту:**
   ```bash
   ls /Users/pavelantipov/telegram-mcp/main.py
   ```

5. **Проверьте .env файл:**
   ```bash
   ls /Users/pavelantipov/telegram-mcp/.env
   ```

## 🔄 Альтернатива: Подключение к Render серверу

Если локальный запуск не работает, можно подключиться к серверу на Render:

Обновите `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "telegram-mcp": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://telegram-mcp-n302.onrender.com/sse"
      ]
    }
  }
}
```

**Примечание:** Для этого варианта нужен установленный `npx` (часть Node.js).

## 📝 Текущая конфигурация

```json
{
  "mcpServers": {
    "telegram-mcp": {
      "command": "/Users/pavelantipov/.local/bin/uv",
      "args": [
        "--directory",
        "/Users/pavelantipov/telegram-mcp",
        "run",
        "main.py"
      ]
    }
  }
}
```

Эта конфигурация запускает Telegram MCP локально, используя переменные окружения из `.env` файла.

---

**Готово!** После перезапуска Cursor вы сможете использовать Telegram MCP! 🚀

