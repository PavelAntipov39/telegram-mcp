# 📝 Как обновить .env файл с правильными API credentials

## 🔍 Текущая проблема

Ошибка: `The api_id/api_hash combination is invalid`

Это означает, что в файле `.env` указаны неверные или тестовые значения для `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`.

## ✅ Решение

### Шаг 1: Получите реальные API credentials

1. Откройте https://my.telegram.org/apps в браузере
2. Войдите с вашим номером телефона: `+79217100905`
3. Введите код подтверждения из Telegram
4. Если нужно, создайте новое приложение:
   - **App title:** `Telegram MCP` (любое название)
   - **Short name:** `telegram_mcp` (любое короткое имя)
   - **Platform:** Desktop
5. Скопируйте:
   - **api_id** — это число (например: `12345678`)
   - **api_hash** — это строка из 32 символов (например: `abcdef1234567890abcdef1234567890`)

### Шаг 2: Откройте файл .env

Откройте файл в редакторе:
```bash
nano /Users/pavelantipov/telegram-mcp/.env
```

Или откройте в Cursor/VS Code:
```bash
code /Users/pavelantipov/telegram-mcp/.env
```

### Шаг 3: Обновите значения

Замените строки:

```env
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=0123456789...
```

На ваши реальные значения (БЕЗ кавычек):

```env
TELEGRAM_API_ID=ваш_реальный_api_id
TELEGRAM_API_HASH=ваш_реальный_api_hash
```

**Пример правильного формата:**
```env
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
TELEGRAM_SESSION_NAME=anon
TELEGRAM_SESSION_STRING=
```

### Шаг 4: Сохраните файл

Сохраните изменения в файле.

### Шаг 5: Запустите генератор сессии снова

```bash
cd /Users/pavelantipov/telegram-mcp
uv run session_string_generator.py
```

Теперь должно работать! Введите:
- Номер телефона: `+79217100905`
- Код подтверждения из Telegram

## ⚠️ Важно

- **НЕ используйте кавычки** вокруг значений в .env файле
- **НЕ добавляйте пробелы** вокруг знака `=`
- **API_ID** — это число, не строка
- **API_HASH** — это строка из 32 символов (буквы и цифры)

## 🔍 Проверка

После обновления .env файла, проверьте значения:

```bash
cd /Users/pavelantipov/telegram-mcp
uv run python3 -c "from dotenv import load_dotenv; import os; load_dotenv(); print('API_ID:', os.getenv('TELEGRAM_API_ID')); print('API_HASH:', os.getenv('TELEGRAM_API_HASH')[:20] + '...')"
```

Если значения правильные, генератор сессии должен работать!

