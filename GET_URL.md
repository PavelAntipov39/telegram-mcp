# Как получить публичный URL для MCP-сервера

## 📍 Текущий статус

✅ Все файлы настроены:
- `Dockerfile` - готов для деплоя с SSE
- `docker-compose.yml` - для локального тестирования
- `.dockerignore` - оптимизирован
- Документация по деплою готова

## 🚀 Шаги для получения публичного URL

### Вариант 1: Render.com (Рекомендуется, бесплатный тариф)

1. **Откройте [render.com](https://render.com)** и войдите через GitHub

2. **Создайте Web Service:**
   ```
   New + → Web Service → Connect GitHub → chigwell/telegram-mcp
   ```

3. **Настройки сервиса:**
   - **Name:** `telegram-mcp` (или любое имя)
   - **Environment:** `Docker`
   - **Region:** `Singapore` (или ближайший)
   - **Branch:** `main`
   - **Root Directory:** (оставьте пустым)

4. **Добавьте Environment Variables:**
   В разделе "Environment Variables" добавьте:
   ```
   TELEGRAM_API_ID = <ваш_api_id>
   TELEGRAM_API_HASH = <ваш_api_hash>
   TELEGRAM_SESSION_STRING = <ваш_session_string>
   PORT = 8080
   ```

5. **Нажмите "Create Web Service"**

6. **Дождитесь деплоя (2-5 минут)**

7. **Получите URL:**
   После успешного деплоя Render покажет:
   ```
   https://telegram-mcp-xxxx.onrender.com
   ```
   
   **Ваш SSE endpoint для ChatGPT:**
   ```
   https://telegram-mcp-xxxx.onrender.com/sse
   ```

### Вариант 2: Railway.app

1. Откройте [railway.app](https://railway.app)
2. New Project → Deploy from GitHub repo → выберите `chigwell/telegram-mcp`
3. Добавьте переменные окружения в разделе "Variables"
4. После деплоя URL будет: `https://<project-name>.railway.app/sse`

### Вариант 3: Northflank

1. Откройте [northflank.com](https://northflank.com)
2. Создайте проект → Add Service → Docker
3. Подключите GitHub репозиторий
4. URL будет: `https://<service-name>.northflank.app/sse`

## ✅ Проверка URL

После деплоя проверьте endpoint:

```bash
curl -I https://<your-host>/sse
```

Ожидаемый ответ:
```
HTTP/1.1 200 OK
Content-Type: text/event-stream
Cache-Control: no-cache
Connection: keep-alive
```

## 🔗 Использование в ChatGPT

1. Откройте ChatGPT → Settings → Connectors
2. Create → MCP Server
3. Заполните:
   - **Transport:** SSE
   - **URL:** `https://<your-host>/sse`
   - **Auth:** None
4. Сохраните

## 📝 Важно

- Убедитесь, что изменения закоммичены и запушены в GitHub перед деплоем
- После первого деплоя Render может "засыпать" после 15 минут бездействия (бесплатный тариф)
- Для production используйте платный тариф или другой PaaS

## 🐛 Если URL не работает

1. Проверьте логи в панели PaaS
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что порт правильно настроен (8080)
4. Убедитесь, что endpoint заканчивается на `/sse`

